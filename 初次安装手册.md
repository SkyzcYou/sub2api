# xingliux 初次安装手册

本文档记录 `xingliux` 定制镜像首次部署到生产 VPS 的实际过程，并提供可重复执行的安装步骤。文档不记录任何密码、令牌或密钥值。

## 部署目标

- VPS SSH 别名：`xingliux-vps`
- VPS 地址：`15.204.80.168`
- VPS 用户：`skyzcyou`
- 部署目录：`/home/skyzcyou/Applications/xingliux`
- Compose 文件：`deploy/docker-compose.yml`
- 应用镜像：`crpi-b1po1b8mfjqfuj2k.cn-shenzhen.personal.cr.aliyuncs.com/skyzcstack/xingliux:latest`
- 依赖服务：`postgres:18-alpine`、`redis:8-alpine`

Compose 文件使用命名卷保存 `sub2api`、PostgreSQL 和 Redis 数据；应用首次启动时通过 `AUTO_SETUP=true` 自动完成数据库初始化。

## 前置条件

VPS 需要具备：

- Ubuntu/Linux 主机
- Docker Engine 和 Docker Compose v2 插件
- 能访问 Docker Hub 和阿里云 ACR
- ACR 访问凭证（密码不写入文档）
- 本地开发机已配置 `xingliux-vps` SSH 别名，并执行过：

```bash
chmod 600 ~/.ssh/config
ssh xingliux-vps
```

## 从本地上传部署文件

在本地仓库根目录执行。这里上传的是工作区中已经指向 ACR 的 Compose 文件：

```bash
cd /home/skyzcyou/Repos/GoProjects/sub2api
scp deploy/docker-compose.yml deploy/.env.example \
  xingliux-vps:/home/skyzcyou/Applications/xingliux/
```

连接到 VPS 并确认目录：

```bash
ssh xingliux-vps
cd /home/skyzcyou/Applications/xingliux
ls -la
```

检查 Docker 版本和主机资源：

```bash
docker --version
docker compose version
free -h
df -h /
```

## 创建生产环境文件

首次安装时从模板创建 `.env`，并限制文件权限：

```bash
cd /home/skyzcyou/Applications/xingliux
cp .env.example .env
chmod 600 .env
```

生成随机凭证。生成的值应保存到密码管理器：

```bash
POSTGRES_PASSWORD=$(openssl rand -hex 32)
REDIS_PASSWORD=$(openssl rand -hex 32)
JWT_SECRET=$(openssl rand -hex 32)
TOTP_ENCRYPTION_KEY=$(openssl rand -hex 32)
ADMIN_PASSWORD=$(openssl rand -hex 20)
```

将凭证写入 `.env`，并将应用绑定到本机回环地址，后续由 Nginx/Caddy 提供 HTTPS：

```bash
sed -i \
  -e "s/^POSTGRES_PASSWORD=.*/POSTGRES_PASSWORD=${POSTGRES_PASSWORD}/" \
  -e "s/^REDIS_PASSWORD=.*/REDIS_PASSWORD=${REDIS_PASSWORD}/" \
  -e "s/^JWT_SECRET=.*/JWT_SECRET=${JWT_SECRET}/" \
  -e "s/^TOTP_ENCRYPTION_KEY=.*/TOTP_ENCRYPTION_KEY=${TOTP_ENCRYPTION_KEY}/" \
  -e "s/^ADMIN_PASSWORD=.*/ADMIN_PASSWORD=${ADMIN_PASSWORD}/" \
  -e 's/^BIND_HOST=.*/BIND_HOST=127.0.0.1/' \
  .env
```

设置真实的管理员邮箱：

```bash
vim .env
```

修改 `ADMIN_EMAIL`，并确认 `POSTGRES_PASSWORD`、`JWT_SECRET`、`TOTP_ENCRYPTION_KEY` 和 `ADMIN_PASSWORD` 均已设置。

### 官方数据库参数

首次部署最终恢复为项目 `.env.example` 中的官方参数：

```env
POSTGRES_MAX_CONNECTIONS=1024
POSTGRES_SHARED_BUFFERS=1GB
POSTGRES_EFFECTIVE_CACHE_SIZE=4GB
POSTGRES_MAINTENANCE_WORK_MEM=128MB
DATABASE_MAX_OPEN_CONNS=256
DATABASE_MAX_IDLE_CONNS=128
```

这些值位于 `deploy/.env.example`，其中部分注释以约 8GB 内存机器作为参考。若 VPS 资源较小，应在确认内存和并发需求后再调整；不要在数据库已经初始化后随意重新生成 `POSTGRES_PASSWORD`。

## 登录 ACR

`xingliux` 是阿里云私有仓库镜像。必须在 VPS 上使用 ACR 访问凭证登录：

```bash
docker login --username=15985428639 \
  crpi-b1po1b8mfjqfuj2k.cn-shenzhen.personal.cr.aliyuncs.com
```

密码由 Docker 交互式提示输入，不要写入命令行、脚本或本文档。

## 校验并启动

在 Compose 文件所在目录执行：

```bash
docker compose config --quiet
docker compose pull
docker compose up -d
```

检查服务和健康状态：

```bash
docker compose ps
docker compose logs --tail=100 sub2api
curl -fsS http://127.0.0.1:8080/health
```

应用未设置 `ADMIN_PASSWORD` 时会在首次启动日志中输出自动生成的管理员密码；本次安装流程使用固定随机值，因此应从密码管理器取值登录。

如果 `BIND_HOST=127.0.0.1` 且尚未配置反向代理，可用 SSH 隧道临时访问：

```bash
ssh -N -L 8080:127.0.0.1:8080 xingliux-vps
```

然后在本地浏览器打开 `http://127.0.0.1:8080`。正式对外服务前必须配置域名、HTTPS 和反向代理；不要直接长期暴露未加密的 8080 端口。

## 本次实际操作记录

1. 在本地配置 `xingliux-vps` SSH 别名，并验证免密登录成功。
2. 将 `deploy/docker-compose.yml` 和 `deploy/.env.example` 上传到 `/home/skyzcyou/Applications/xingliux`。
3. 从 `.env.example` 创建 `.env`，生成并写入 PostgreSQL、Redis、JWT、TOTP 和管理员密码。
4. 初始阶段曾将数据库参数临时设为较保守值：`200`、`256MB`、`1GB`、`100`、`20`。
5. 根据项目官方模板要求，将上述 5 个参数恢复为 `1024`、`1GB`、`4GB`、`256`、`128`。
6. 恢复操作前创建了远端备份：`.env.bak.20260802182506`。
7. `docker compose config --quiet` 通过。
8. 首次执行 `docker compose up -d` 时，PostgreSQL 和 Redis 镜像开始拉取，但 ACR 应用镜像因 VPS 尚未登录私有仓库而失败：

```text
pull access denied ... repository does not exist or may require 'docker login'
```

9. 失败发生在镜像拉取阶段，未创建应用服务容器；完成 ACR 登录后重新执行“校验并启动”步骤即可。

## 日常运维

查看状态和日志：

```bash
docker compose ps
docker compose logs -f sub2api
```

发布新镜像后更新：

```bash
docker compose pull
docker compose up -d
```

停止服务但保留数据：

```bash
docker compose down
```

不要执行以下命令，除非已确认要删除全部数据：

```bash
docker compose down -v
```

## 故障排查

### ACR `pull access denied`

确认 Docker 登录的是正确的 Registry 域名，并重新执行：

```bash
docker login --username=15985428639 \
  crpi-b1po1b8mfjqfuj2k.cn-shenzhen.personal.cr.aliyuncs.com
docker compose pull
```

### Compose 提示 `POSTGRES_PASSWORD is required`

确认当前目录是 `/home/skyzcyou/Applications/xingliux`，并检查 `.env` 是否存在且包含非空的 `POSTGRES_PASSWORD`：

```bash
pwd
test -f .env && echo '.env exists'
grep '^POSTGRES_PASSWORD=' .env | sed 's/=.*/=<redacted>/'
```

### 服务启动但健康检查失败

按依赖顺序查看日志：

```bash
docker compose logs --tail=200 postgres
docker compose logs --tail=200 redis
docker compose logs --tail=200 sub2api
```

确认 PostgreSQL 和 Redis 先达到 `healthy`，再检查应用的 `/health` 响应。
